[package]
name = "seed"
version = "0.1.1"
edition = "2024"
publish = false

[dependencies]
clap = { version = "=4.6.6", features = ["derive"] }
dialoguer = { version = "=0.12.0", default-features = false }

[dev-dependencies]
tempfile = "3"
